import React from 'react';

const  SeasonDisplay = (props) => {
    console.log(props.lat);
        return (
            <div>
                hello there
            </div>
        );
}

export default SeasonDisplay;